import Login from "@/pages/auth/login";

const page = () => {
  return (
    <div className=" container max-w-full flex items-center justify-center h-screen fixed top-0 ">
      <Login />
    </div>
  );
};

export default page;
